import React from 'react';
import { LoadingStyles, LoadingCircle } from './LoadingStyles';

function Loading() {
  return (
    <LoadingStyles>
      <LoadingCircle />
    </LoadingStyles>
  );
}

export default Loading;
